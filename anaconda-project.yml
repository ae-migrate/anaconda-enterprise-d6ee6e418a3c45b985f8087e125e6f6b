name: R Shiny Demo
description: An example R Shiny app

commands:
  default:
    unix: Rscript run_shiny.R
    supports_http_options: true

packages:
  - r-argparse
  - r-shiny
  - python

platforms:
  - linux-64

env_specs:
  anaconda50_r: {}
